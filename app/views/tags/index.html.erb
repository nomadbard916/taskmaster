<div class="container">
    <div class="row" id="heading">
        <h3>Add tags to your task:</h3>
    </div>

    <div class="form-group row" id="tag-adding">
        <%= form_for [@task, @tag] do |f| %>
        <fieldset>
            <%= f.text_field :content, placeholder: "maximum 16 characters" %>
            <%= f.submit %>
        </fieldset>
        <% end %>
    </div>

    <hr>
    <div class="row">
        <h5>Tags for this task:</h5>
    </div>

    <br>


</div>